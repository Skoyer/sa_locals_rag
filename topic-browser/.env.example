# Copy these into the repository root `.env` (Vite loads `envDir: '..'` from `topic-browser`).
# Or set `VITE_SUPABASE_*` in any `.env` / `.env.local` file in the parent folder.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
